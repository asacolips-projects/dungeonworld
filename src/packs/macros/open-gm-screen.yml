_id: LLyVgwxC4J4p8N7S
name: Open GM Screen
permission:
  default: 0
type: script
flags:
  core:
    sourceId: Macro.hDvKOW2WrNcTyd69
scope: global
command: |-
  /*
  source: 
  icon:
  */
  (async () => {
    let list_compendium = await game.packs.filter(p=>p.entity=='JournalEntry' );
    let inside = await list_compendium.filter( p=>p.metadata.label=='GM Screen' )[0].getContent();
    let journal = await inside.filter( p=>p._data['name']=='!GM Screen' )[0];

    if (journal.sheet.rendered)  
      journal.sheet.close(); 
    else 
      journal.sheet.render(true);
    
  })()
author: 1naqx1UoDKZUy10j
img: systems/dungeonworld/assets/icons/macros/gmscreen.svg
actorIds: []
